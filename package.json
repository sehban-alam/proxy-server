{
	"name": "proxy-server",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"prettier": "npx prettier . --write"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.5.2",
		"prettier": "3.3.3",
		"vitest": "2.0.5",
		"wrangler": "^3.60.3"
	}
}
